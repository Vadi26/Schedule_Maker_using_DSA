#include<stdio.h>
#include<stdlib.h>
#include<string.h>

int main() {
    char *name = "VADiraj";
    if (!strcmp(name, "VADiraj")) printf("BOOOYAAA");
    // if (name == "VADiraj") printf("%s ", name);

    return 0;
}